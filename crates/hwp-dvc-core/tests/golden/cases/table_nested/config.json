{
  "fixture": "table_nested.hwpx",
  "spec": "fixture_spec.json",
  "description": "Fixture with a nested table whose outer and inner tables both have treatAsChar=\"0\" — expected to produce TABLE_TREAT_AS_CHAR (3004) on both tables and TABLE_IN_TABLE (3056) on the inner table."
}
